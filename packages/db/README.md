# Package - DB

Prisma schema, client, and migration utilities for the real estate database.

## Migrations

```bash
npm run migrate:generate      # Create a new migration
npm run migrate:deploy        # Apply pending migrations
npm run migrate:reset         # Reset database and re-apply all migrations
npm run db:codegen            # Regenerate Prisma client
```
