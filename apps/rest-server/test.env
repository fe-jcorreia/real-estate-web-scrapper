PORT=4001
NPM_CONFIG_PRODUCTION=false

# Possible values (in order): debug, info, warn, error, critical
LOGGER_LEVEL=critical

CRYPTO_SALT=salt

# DATABASE_URL="<dialect>://<username>:<password>@<host>:<port>/<db_name>"
DATABASE_URL="postgres://postgres:postgres@localhost:5433/template_test"

JWT_EXPIRATION=1d
JWT_SECRET=secret

OPEN_API_SCHEMA_VISIBLE=false
